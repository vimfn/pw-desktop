{
  "name": "physicswallah-desktop",
  "private": true,
  "version": "1.0.2-beta",
  "type": "module",
  "scripts": {
    "update": "pnpm up --latest && cd src-tauri && cargo update && cd ..",
    "dev": "tauri dev",
    "build:deb": "tauri build --bundles deb",
    "build:msi": "tauri build --bundles msi"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.4.0"
  }
}
